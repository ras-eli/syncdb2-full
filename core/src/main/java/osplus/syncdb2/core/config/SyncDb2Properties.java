package osplus.syncdb2.core.config;

/**
 * Zweck
 * -----
 * Konfigurations-Skelett für das syncdb2-Framework. In einer Spring-Boot-Umgebung
 * wird dieses Objekt über @ConfigurationProperties (Prefix: "syncdb2") befüllt.
 * <p>
 * Hinweise
 * --------
 * - Konkrete Felder und verschachtelte Klassen (mq.send/read, logging.maskedParams etc.)
 * werden in einem Folgeschritt präzisiert. Dieses Skelett hält nur den Platz.
 */
public final class SyncDb2Properties {
    // TODO: Felder + Getter/Setter oder Record mit verschachtelten Typen
}
